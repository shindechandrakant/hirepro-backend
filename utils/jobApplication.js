export const jobApplicationUtil = async (fields, files) => {
  //
  try {
    console.log({ files });
    console.log({ fields });
  } catch (error) {
    console.log("Error occured while applying job");
  }
};
